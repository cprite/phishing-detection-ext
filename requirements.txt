# Training only (train_browser.py / retrain_from_feedback.py) — not needed to
# run the extension.
pandas
numpy
scikit-learn
skl2onnx
onnxruntime
# retrain_from_feedback.py fetches pages to count links (best effort).
requests
beautifulsoup4
